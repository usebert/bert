# Copy to `keystore.properties` in this directory (android/) and fill in values.
# Never commit `keystore.properties` or your keystore to version control.
#
# Paths for storeFile are relative to the android/ project directory unless absolute.

storePassword=your-store-password
keyPassword=your-key-password
keyAlias=release
storeFile=release.keystore
